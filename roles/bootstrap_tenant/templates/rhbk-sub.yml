apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: rhbk-sub-{{ quarkus_on_openshift_bootstrap_tenant_username }}
  namespace: "{{ item }}"
spec:
  channel: stable-v24
  installPlanApproval: Automatic
  name: rhbk-operator
  source: redhat-operators-index
  sourceNamespace: openshift-marketplace
